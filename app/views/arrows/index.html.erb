<nav class="navbar navbar-expand-md navbar-dark bg-primary">
  <%= link_to "Icalia Labs", root_path, class: "navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample04" aria-controls="navbarsExample04" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarsExample04">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to "Home", root_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "Edit User", edit_user_registration_path, class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "Sign out", destroy_user_session_path, :method=>'delete', class: "nav-link" %>
      </li>
    </ul>
  </div>
</nav>

<br />
<div class="container">
  <div class="card">
    <div class="card-body">
      <div class="row">
        <div class="col-md-2 align-self-center">
          <%= image_tag("avatar.png", alt: "Login Logo", size: '150') %>
        </div>
        <div class="col-md-10">
          <h4 class="text-capitalize text-muted"><b>Icalier: <%= current_user.name %></b></h4>
          <h5 class="text-warning"><b>123 Points</b></h5>
          <a href="#" class="btn btn-primary" data-toggle="modal" data-target="#createArrowModal">Create arrow</a>
        </div>
      </div>
    </div>
  </div>
</div>

<br />
<div class="container">
  <div class="card border border-white">
    <div class ="row">
      <div class="card-body">    
        <% @arrows.each do |arrow| %>
          <div class="card col-md-4">
            <div class="card-body">
              <span>DATE: <%= ActiveSupport::TimeZone["Central Time (US & Canada)"].parse(arrow.created_at.to_s).utc.to_date.strftime("%m/%b/%Y")[1..-1] %></span><br /><br />
              <%= image_tag("avatar.png", alt: "Login Logo", size: '50') %><br /><br />
              <a href="<%= "arrows/#{arrow.id}" %>" class="btn btn-warning btn-small btn-block">More info</a>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>


<!-- Modal -->
<div class="modal fade" id="createArrowModal" tabindex="-1" role="dialog" aria-labelledby="createArrowModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="createArrowModalLabel">Create an Arrow</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <%= form_for @arrow, :url => url_for(:controller => 'arrows', :action => 'create') do |f| %>
          <%= f.label 'FOR WHO?' %><br />
          <%= f.collection_select :to_user_id, User.where.not(id: current_user.id).order(:name),:id,:name, {}, class: "form-control" %><br />
          <%= f.label 'REASON' %><br />
          <%= f.text_area :reason, class: 'form-control' %><br />
          <%= f.hidden_field :user_id, value: current_user.id %><br />
          <%= f.submit 'Send', class: 'btn btn-primary' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
